# Programs that `make test-native` leaves out, one per line: what a program
# runeopt made does not do yet (docs/plans/codegen.md):
#
#   NAME CATEGORY: reason
#
# A category stays only while a program is under it.
#
# RESTORE: carrying on an image as native code is M9 of the plan. A native
# program saves as runevm does, and runevm resumes what it saved (the .restore
# tests do that), but the child of an emulated fork is such an image.
rt.fork_image RESTORE: the child of a fork by a second VM resumes an image
