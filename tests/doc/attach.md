# signature ATTACH

[Library](../README.md) &rsaquo; **ATTACH**

|  |  |
| --- | --- |
| Status | required |
| Implementations | none |
| Documentation | 9 of 11 entries documented |
| Tests | not listed |
| Source | [tests/doc/attach.sml](../tests/doc/attach.sml) |

## Synopsis

```sml
signature ATTACH
```

The signature's own comment: it stands directly above the declaration, and
it is the first comment of the file.

A second paragraph belongs to it as well.

## Contents

[Types](#types) &middot;
[Values](#values)

## Interface

<pre>
signature ATTACH =
sig

  type <a href="#type-t">t</a>

  datatype <a href="#type-colour">colour</a> =
      <a href="#con-red">Red</a>
    | <a href="#con-green">Green</a> of int
    | <a href="#con-blue">Blue</a> of {<a href="#fld-blue.depth">depth</a> : int,
               <a href="#fld-blue.alpha">alpha</a> : real,
               <a href="#fld-blue.name">name</a> : string}
    | <a href="#con-black">Black</a>

  exception <a href="#exn-failed">Failed</a> of {<a href="#fld-failed.why">why</a> : string, <a href="#fld-failed.code">code</a> : int}

  val <a href="#val-first">first</a> : t
  val <a href="#val-second">second</a> : t

  val <a href="#val-third">third</a> : t
  and <a href="#val-fourth">fourth</a> : t
  and <a href="#val-fifth">fifth</a> : t

  val <a href="#val-record">record</a> : {<a href="#fld-record.x">x</a> : int,
                <a href="#fld-record.y">y</a> : int} -&gt; t

  structure <a href="#str-inner">Inner</a> :
  sig

    val <a href="#val-inner.deep">deep</a> : t

  end

  include ORD

end
</pre>

## Types

### <a name="type-t"></a>`t`

```sml
type t
```

A comment above a specification documents it.

### <a name="type-colour"></a>`colour`

```sml
datatype colour =
    Red
  | Green of int
  | Blue of {depth : int,
             alpha : real,
             name : string}
  | Black
```

Above a datatype: the datatype.

| Constructor | Argument | Description |
| --- | --- | --- |
| <a name="con-red"></a>`Red` |  | after a constructor, on its line |
| <a name="con-green"></a>`Green` | `int` | the bar of the next line does not matter |
| <a name="con-blue"></a>`Blue` | `{depth : int, alpha : real, name : string}` | on a line of its own inside a datatype: the next constructor |
| &nbsp;&nbsp;&nbsp;&nbsp;<a name="fld-blue.depth"></a>`depth` | `int` | after a field and its comma |
| &nbsp;&nbsp;&nbsp;&nbsp;<a name="fld-blue.alpha"></a>`alpha` | `real` | above a field |
| &nbsp;&nbsp;&nbsp;&nbsp;<a name="fld-blue.name"></a>`name` | `string` | after the last field, before the brace |
| <a name="con-black"></a>`Black` |  | after the last constructor: the constructor, not the datatype |

### <a name="exn-failed"></a>`Failed`

```sml
exception Failed of {why : string, code : int}
```

after the brace: the exception

| Field | Type | Description |
| --- | --- | --- |
| <a name="fld-failed.why"></a>`why` | `string` | why it failed |
| <a name="fld-failed.code"></a>`code` | `int` |  |

## Values

This paragraph stands for itself: a blank line follows it. It belongs to
the section Values.

### <a name="val-first"></a>`first`

```sml
val first : t
```

Above the first of two.

### <a name="val-second"></a>`second`

```sml
val second : t
```

after a value

### <a name="val-third"></a>`third`

```sml
val third : t
```

### <a name="val-fourth"></a>`fourth`

```sml
val fourth : t
```

after a description that follows `and`

### <a name="val-fifth"></a>`fifth`

```sml
val fifth : t
```

above an `and`

### <a name="val-record"></a>`record`

```sml
val record : {x : int,
              y : int} -> t
```

| Field | Type | Description |
| --- | --- | --- |
| <a name="fld-record.x"></a>`x` | `int` | the x |
| <a name="fld-record.y"></a>`y` | `int` |  |

### <a name="str-inner"></a>`Inner`

Above a substructure with a signature of its own.

## Inside

#### <a name="val-inner.deep"></a>`deep`

```sml
val deep : t
```

Above a value of the substructure.

The last words of the substructure.

**Included from `ORD`**: `include ORD`

Above an include.

At the end of the signature: prose as well.

---

<sub>Generated by runedoc from tests/doc/attach.sml; do not edit.</sub>

