# signature ORDERED

[Library](../README.md) &rsaquo; **ORDERED**

|  |  |
| --- | --- |
| Status | required |
| Documentation | 0 of 2 entries documented |
| Source | [tests/doc/specs.sml](../tests/doc/specs.sml) |

## Synopsis

```sml
signature ORDERED
```

Every kind of specification, and the derived forms the parser rewrites.

## Interface

<pre>
signature ORDERED =
sig
  type <a href="#type-t">t</a>
  val <a href="#val-compare">compare</a> : t * t -&gt; order
end
</pre>

### <a name="type-t"></a>`t`

```sml
type t
```

### <a name="val-compare"></a>`compare`

```sml
val compare : t * t -> order
```

---

<sub>Generated by runedoc from tests/doc/specs.sml; do not edit.</sub>

# signature SPECS

[Library](../README.md) &rsaquo; **SPECS**

|  |  |
| --- | --- |
| Status | required |
| Documentation | 0 of 22 entries documented |
| Source | [tests/doc/specs.sml](../tests/doc/specs.sml) |

## Synopsis

```sml
signature SPECS
```

## Interface

<pre>
signature SPECS =
sig
  type 'a <a href="#type-seq">seq</a>
  eqtype <a href="#type-key">key</a>
  type <a href="#type-pair">pair</a> = key * int and 'a <a href="#type-box">box</a> = {<a href="#fld-box.contents">contents</a> : 'a, <a href="#fld-box.tag">tag</a> : key}
  datatype 'a <a href="#type-tree">tree</a> = <a href="#con-leaf">Leaf</a> | <a href="#con-node">Node</a> of 'a tree * 'a * 'a tree
  datatype <a href="#type-shape">shape</a> =
      <a href="#con-circle">Circle</a> of {<a href="#fld-circle.centre">centre</a> : real * real, <a href="#fld-circle.radius">radius</a> : real}
    | <a href="#con-box">Box</a> of {<a href="#fld-box.corner">corner</a> : real * real,
              <a href="#fld-box.width">width</a> : real,
              <a href="#fld-box.height">height</a> : real}
  datatype <a href="#type-order">order</a> = datatype order
  exception <a href="#exn-empty">Empty</a>
  exception <a href="#exn-bad">Bad</a> of {<a href="#fld-bad.line">line</a> : int, <a href="#fld-bad.why">why</a> : string} and <a href="#exn-worse">Worse</a> of string

  val <a href="#val-empty">empty</a> : 'a seq
  val <a href="#val-insert">insert</a> : key * 'a -&gt; 'a seq -&gt; 'a seq
  and <a href="#val-remove">remove</a> : key -&gt; 'a seq -&gt; 'a seq
  val op <a href="#val-op-at-at">@@</a> : 'a seq * 'a seq -&gt; 'a seq
  val <a href="#val-fold">fold</a> : ({<a href="#fld-fold.key">key</a> : key, <a href="#fld-fold.value">value</a> : 'a} * 'b -&gt; 'b) -&gt; 'b -&gt; 'a seq -&gt; 'b
  val <a href="#val-find">find</a> : ('a -&gt; bool) -&gt; 'a seq -&gt; 'a option

  structure <a href="#str-key">Key</a> : ORDERED where type t = key
  structure <a href="#str-limits">Limits</a> : sig val <a href="#val-limits.maxlen">maxLen</a> : int  val <a href="#val-limits.minlen">minLen</a> : int end
  structure <a href="#str-a">A</a> : ORDERED and <a href="#str-b">B</a> : ORDERED
  sharing type A.t = B.t

  include ORDERED
  include ORDERED where type t = key
end
</pre>

### <a name="type-seq"></a>`seq`

```sml
type 'a seq
```

### <a name="type-key"></a>`key`

```sml
eqtype key
```

### <a name="type-pair"></a>`pair`

```sml
type pair = key * int
```

### <a name="type-box"></a>`box`

```sml
type 'a box = {contents : 'a, tag : key}
```

| Field | Type | Description |
| --- | --- | --- |
| <a name="fld-box.contents"></a>`contents` | `'a` |  |
| <a name="fld-box.tag"></a>`tag` | `key` |  |

### <a name="type-tree"></a>`tree`

```sml
datatype 'a tree = Leaf | Node of 'a tree * 'a * 'a tree
```

| Constructor | Argument | Description |
| --- | --- | --- |
| <a name="con-leaf"></a>`Leaf` |  | nothing |
| <a name="con-node"></a>`Node` | `'a tree * 'a * 'a tree` |  |

### <a name="type-shape"></a>`shape`

```sml
datatype shape =
    Circle of {centre : real * real, radius : real}
  | Box of {corner : real * real,
            width : real,
            height : real}
```

| Constructor | Argument | Description |
| --- | --- | --- |
| <a name="con-circle"></a>`Circle` | `{centre : real * real, radius : real}` |  |
| &nbsp;&nbsp;&nbsp;&nbsp;<a name="fld-circle.centre"></a>`centre` | `real * real` |  |
| &nbsp;&nbsp;&nbsp;&nbsp;<a name="fld-circle.radius"></a>`radius` | `real` |  |
| <a name="con-box"></a>`Box` | `{corner : real * real, width : real, height : real}` |  |
| &nbsp;&nbsp;&nbsp;&nbsp;<a name="fld-box.corner"></a>`corner` | `real * real` | lower left |
| &nbsp;&nbsp;&nbsp;&nbsp;<a name="fld-box.width"></a>`width` | `real` |  |
| &nbsp;&nbsp;&nbsp;&nbsp;<a name="fld-box.height"></a>`height` | `real` |  |

### <a name="type-order"></a>`order`

```sml
datatype order = datatype order
```

### <a name="exn-empty"></a>`Empty`

```sml
exception Empty
```

### <a name="exn-bad"></a>`Bad`

```sml
exception Bad of {line : int, why : string}
```

| Field | Type | Description |
| --- | --- | --- |
| <a name="fld-bad.line"></a>`line` | `int` |  |
| <a name="fld-bad.why"></a>`why` | `string` |  |

### <a name="exn-worse"></a>`Worse`

```sml
exception Worse of string
```

### <a name="val-empty"></a>`empty`

```sml
val empty : 'a seq
```

### <a name="val-insert"></a>`insert`

```sml
val insert : key * 'a -> 'a seq -> 'a seq
```

### <a name="val-remove"></a>`remove`

```sml
val remove : key -> 'a seq -> 'a seq
```

### <a name="val-op-at-at"></a>`@@`

```sml
val op @@ : 'a seq * 'a seq -> 'a seq
```

### <a name="val-fold"></a>`fold`

```sml
val fold : ({key : key, value : 'a} * 'b -> 'b) -> 'b -> 'a seq -> 'b
```

| Field | Type | Description |
| --- | --- | --- |
| <a name="fld-fold.key"></a>`key` | `key` |  |
| <a name="fld-fold.value"></a>`value` | `'a` |  |

### <a name="val-find"></a>`find`

```sml
val find : ('a -> bool) -> 'a seq -> 'a option
```

### <a name="str-key"></a>`Key`

```sml
structure Key : ORDERED where type t = key
```

A substructure: its members are described on the page of [`ORDERED`](../sig/ORDERED.md).

### <a name="str-limits"></a>`Limits`

#### <a name="val-limits.maxlen"></a>`maxLen`

```sml
val maxLen : int
```

#### <a name="val-limits.minlen"></a>`minLen`

```sml
val minLen : int
```

### <a name="str-a"></a>`A`

```sml
structure A : ORDERED
```

A substructure: its members are described on the page of [`ORDERED`](../sig/ORDERED.md).

### <a name="str-b"></a>`B`

```sml
structure B : ORDERED
```

A substructure: its members are described on the page of [`ORDERED`](../sig/ORDERED.md).

**Included from [`ORDERED`](../sig/ORDERED.md)**: `include ORDERED`

| Member |  |  |
| --- | --- | --- |
| [`t`](../sig/ORDERED.md#type-t) | type |  |
| [`compare`](../sig/ORDERED.md#val-compare) | val |  |

**Included from [`ORDERED`](../sig/ORDERED.md)**: `include ORDERED where type t = key`

| Member |  |  |
| --- | --- | --- |
| [`t`](../sig/ORDERED.md#type-t) | type |  |
| [`compare`](../sig/ORDERED.md#val-compare) | val |  |

---

<sub>Generated by runedoc from tests/doc/specs.sml; do not edit.</sub>

# signature INCLUDES

[Library](../README.md) &rsaquo; **INCLUDES**

|  |  |
| --- | --- |
| Status | required |
| Documentation | 0 of 1 entries documented |
| Source | [tests/doc/specs.sml](../tests/doc/specs.sml) |

## Synopsis

```sml
signature INCLUDES
```

## Interface

<pre>
signature INCLUDES =
sig
  include ORDERED SPECS
  include sig val <a href="#val-extra">extra</a> : int end
end
</pre>

**Included from [`ORDERED`](../sig/ORDERED.md)**: `include ORDERED`

| Member |  |  |
| --- | --- | --- |
| [`t`](../sig/ORDERED.md#type-t) | type |  |
| [`compare`](../sig/ORDERED.md#val-compare) | val |  |

**Included from [`SPECS`](../sig/SPECS.md)**: `include SPECS`

| Member |  |  |
| --- | --- | --- |
| [`seq`](../sig/SPECS.md#type-seq) | type |  |
| [`key`](../sig/SPECS.md#type-key) | eqtype |  |
| [`pair`](../sig/SPECS.md#type-pair) | type |  |
| [`box`](../sig/SPECS.md#type-box) | type |  |
| [`tree`](../sig/SPECS.md#type-tree) | datatype |  |
| [`shape`](../sig/SPECS.md#type-shape) | datatype |  |
| [`order`](../sig/SPECS.md#type-order) | datatype |  |
| [`Empty`](../sig/SPECS.md#exn-empty) | exception |  |
| [`Bad`](../sig/SPECS.md#exn-bad) | exception |  |
| [`Worse`](../sig/SPECS.md#exn-worse) | exception |  |
| [`empty`](../sig/SPECS.md#val-empty) | val |  |
| [`insert`](../sig/SPECS.md#val-insert) | val |  |
| [`remove`](../sig/SPECS.md#val-remove) | val |  |
| [`@@`](../sig/SPECS.md#val-op-at-at) | val |  |
| [`fold`](../sig/SPECS.md#val-fold) | val |  |
| [`find`](../sig/SPECS.md#val-find) | val |  |
| [`Key`](../sig/SPECS.md#str-key) | structure |  |
| [`Limits`](../sig/SPECS.md#str-limits) | structure |  |
| [`Limits.maxLen`](../sig/SPECS.md#val-limits.maxlen) | val |  |
| [`Limits.minLen`](../sig/SPECS.md#val-limits.minlen) | val |  |
| [`A`](../sig/SPECS.md#str-a) | structure |  |
| [`B`](../sig/SPECS.md#str-b) | structure |  |

### <a name="val-extra"></a>`extra`

```sml
val extra : int
```

---

<sub>Generated by runedoc from tests/doc/specs.sml; do not edit.</sub>

# signature RENAMED

[Library](../README.md) &rsaquo; **RENAMED**

|  |  |
| --- | --- |
| Status | required |
| Documentation | 0 of 0 entries documented |
| Source | [tests/doc/specs.sml](../tests/doc/specs.sml) |

## Synopsis

```sml
signature RENAMED = ORDERED where type t = int
```

---

<sub>Generated by runedoc from tests/doc/specs.sml; do not edit.</sub>

