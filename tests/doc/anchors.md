# signature ANCHORS

[Library](../README.md) &rsaquo; **ANCHORS**

|  |  |
| --- | --- |
| Status | required |
| Documentation | 1 of 9 entries documented |
| Source | [tests/doc/anchors.sml](../tests/doc/anchors.sml) |

## Synopsis

```sml
signature ANCHORS
```

Anchors: symbolic and primed identifiers, members of substructures, and two
names that GitHub cannot tell apart.

## Interface

<pre>
signature ANCHORS =
sig
  val <a href="#val-op-at">@</a> : 'a list * 'a list -&gt; 'a list
  val <a href="#val-op-colon-eq">:=</a> : 'a ref * 'a -&gt; unit
  val <a href="#val-socket">socket</a> : unit -&gt; int
  val <a href="#val-socket-prime">socket'</a> : int -&gt; int
  val <a href="#val-file_desc">file_desc</a> : int
  structure <a href="#str-ctl">Ctl</a> : sig val <a href="#val-ctl.getdebug">getDEBUG</a> : unit -&gt; bool end

  val <a href="#val-tostring">toString</a> : int -&gt; string
  val <a href="#val-tostring">tostring</a> : int -&gt; string
end
</pre>

### <a name="val-op-at"></a>`@`

```sml
val @ : 'a list * 'a list -> 'a list
```

### <a name="val-op-colon-eq"></a>`:=`

```sml
val := : 'a ref * 'a -> unit
```

### <a name="val-socket"></a>`socket`

```sml
val socket : unit -> int
```

### <a name="val-socket-prime"></a>`socket'`

```sml
val socket' : int -> int
```

### <a name="val-file_desc"></a>`file_desc`

```sml
val file_desc : int
```

### <a name="str-ctl"></a>`Ctl`

#### <a name="val-ctl.getdebug"></a>`getDEBUG`

```sml
val getDEBUG : unit -> bool
```

### <a name="val-tostring"></a>`toString`

```sml
val toString : int -> string
```

See [`socket'`](#val-socket-prime), [`Ctl.getDEBUG`](#val-ctl.getdebug), [`@`](#val-op-at) and [`ANCHORS`](ANCHORS.md); `Nowhere.at` leads nowhere.

### <a name="val-tostring"></a>`tostring`

```sml
val tostring : int -> string
```

---

<sub>Generated by runedoc from tests/doc/anchors.sml; do not edit.</sub>

