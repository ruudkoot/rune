# Programs of tests/lang that `make test-windows` leaves out, one per line:
#
#   NAME CATEGORY: reason
#
# Categories:
#   LINUX     what only Linux prints, which a Mac would not print either
#
# Everything else runs on both Windows VMs, bin/runevm.exe and
# bin/runevm32.exe. docs/plans/windows.md is the roadmap that took this list
# from nine programs to the one that only Linux can pass.
basis.posix_linux LINUX: uname says Linux
