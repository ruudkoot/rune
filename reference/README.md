# Reference implementations

Reference implementations are executable behavioral oracles. They may be
simple, slow, or intentionally redundant. Each reference must document the
subset of Standard ML it covers and the reason it is trusted. Keep references
independent from `src/` so comparisons can detect shared implementation bugs.
