# The register instruction set of vm/new. Generated by runeisa from src/isa -- do not edit.
# The description is src/isa/regs.sml, in the language of src/isa/isa.sml.
# fingerprint 00f3834e
# rbc header RUNE\003\000\000\000\116\203\363\000
# Format:  NAME  OPERANDS  DESCRIPTION
# OPERANDS is '-' or a comma separated list of operand names, a list of
# registers last; each operand, and each register of a list, is encoded as a
# little-endian signed 32-bit integer following the opcode byte.
# An opcode's number is its place in this file, from 0.
HALT          -               Stop execution.
MOVE          d,s             Register d := register s.
INT           d,i             Register d := the small integer i.
CONST         d,k             Register d := constant k.
UNIT          d               Register d := unit.
CON0          d,t             Register d := the nullary constructor with tag t.
GLOBAL        d,g             Register d := global g.
SETGLOBAL     g,s             Global g := register s.
ENV           d,e             Register d := slot e of the current closure's environment.
SELF          d               Register d := the closure running.
CALL          f,x             Call the closure in register f with register x; RESULT takes what it returns.
RESULT        d               Register d := what the call or primitive before it left on the stack.
TAILCALL      f,x             Like CALL, but the current frame is replaced.
RET           s               Return register s to the caller.
PRIM          p,d,args...     Register d := primitive p applied to the registers of args.
PRIMPUSH      p,args...       Primitive p applied to the registers of args, its result left for RESULT: for one that saves or restores an image, which resumes at RESULT.
TUPLE         d,n,fields...   Register d := a tuple of the n registers of fields.
CLOSURE       d,f,n,env...    Register d := a closure of function f capturing the n registers of env.
SELECT        d,i,s           Register d := field i of the tuple in register s.
CON           d,t,s           Register d := constructor t applied to register s.
DECON         d,s             Register d := the argument of the constructor value in register s.
CONTAG        d,s             Register d := the tag of the constructor value in register s, as an int.
NEWEXN        d,k             Register d := a fresh exception constructor named by string constant k.
BUILTINEXN    d,i             Register d := built-in exception constructor i (see docs/bytecode.md).
MKEXN         d,c,x           Register d := the exception of the constructor in register c and the payload in register x.
EXNCON        d,s             Register d := the constructor of the exception in register s.
EXNARG        d,s             Register d := the payload of the exception in register s.
SETENV        c,e,v           Slot e of the environment of the closure in register c := register v.
JUMP          o               Jump to absolute code offset o.
JUMPIF        s,o             Jump to o if register s holds true.
JUMPIFNOT     s,o             Jump to o if register s holds false.
JUMPIFNOTTAG  s,o,t           Jump to o unless the constructor value in register s has tag t.
PUSHHANDLER   o               Install an exception handler whose code starts at o, with CATCH.
POPHANDLER    -               Remove the innermost exception handler.
CATCH         d               Register d := the exception a raise left for the handler this begins.
RAISE         s               Raise the exception in register s.
CALLK         f,n,args...     Call function f, known, with the n registers of args, which become its registers 0 to n-1; no closure; RESULT takes what it returns.
TAILCALLK     f,n,args...     Like CALLK, but the current frame is replaced.
