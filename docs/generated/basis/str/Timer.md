# structure Timer

[The Standard ML Basis Library](../README.md) &rsaquo; The operating system &rsaquo; [Structures](../structures.md) &rsaquo; **Timer**

|  |  |
| --- | --- |
| Signature | [`TIMER`](../sig/TIMER.md) |
| Status | required |
| Members | 10 |
| Tests | 14 checks |
| Source | [lib/basis/timer.sml](../../../../lib/basis/timer.sml) |

## Synopsis

```sml
structure Timer : TIMER
```

Timer: how long something took.

## Members

What each means is on [`TIMER`](../sig/TIMER.md); the types are this structure's own.

|  | Member | Is |
| --- | --- | --- |
| type | [`cpu_timer`](../sig/TIMER.md#type-cpu_timer) | *a type of its own* |
| type | [`real_timer`](../sig/TIMER.md#type-real_timer) | *a type of its own* |
| val | [`checkCPUTimer`](../sig/TIMER.md#val-checkcputimer) | `cpu_timer -> {sys : Time.time, usr : Time.time}` |
| val | [`checkCPUTimes`](../sig/TIMER.md#val-checkcputimes) | `cpu_timer -> {gc : {sys : Time.time, usr : Time.time}, nongc : {sys : Time.time, usr : Time.time}}` |
| val | [`checkGCTime`](../sig/TIMER.md#val-checkgctime) | `cpu_timer -> Time.time` |
| val | [`checkRealTimer`](../sig/TIMER.md#val-checkrealtimer) | `real_timer -> Time.time` |
| val | [`startCPUTimer`](../sig/TIMER.md#val-startcputimer) | `unit -> cpu_timer` |
| val | [`startRealTimer`](../sig/TIMER.md#val-startrealtimer) | `unit -> real_timer` |
| val | [`totalCPUTimer`](../sig/TIMER.md#val-totalcputimer) | `unit -> cpu_timer` |
| val | [`totalRealTimer`](../sig/TIMER.md#val-totalrealtimer) | `unit -> real_timer` |

## Notes

### checkCPUTimes

> **Implementation** `Timer.checkCPUTimes/gc-from-the-collector`. The VM adds
> up the processor time of every collection, user and system apart, and
> `gc` is how much of that has passed since the timer started; `nongc` is
> the rest. A collection that grows the heap counts once.

### checkGCTime

> **Implementation** `Timer.checkGCTime/user-time-of-the-collections`. It is
> the `usr` field of what [`checkCPUTimes`](../sig/TIMER.md#val-checkcputimes) reports under `gc`; the system
> time of a collection is in that record and not here.

### totalCPUTimer

> **Implementation** `Timer.totalCPUTimer/from-process-start`. The
> "system-dependent initialization time" is the start of the process, so
> the timer's base is zero processor time and what it reports is what the
> whole run has used.

### totalRealTimer

> **Implementation** `Timer.totalRealTimer/from-initialisation`. It counts
> from the moment the library was initialised, just before the program's
> own code begins.

---

<sub>Generated by runedoc from lib/basis/timer.sml; do not edit.</sub>
