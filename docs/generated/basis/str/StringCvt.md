# structure StringCvt

[The Standard ML Basis Library](../README.md) &rsaquo; Text and characters &rsaquo; [Structures](../structures.md) &rsaquo; **StringCvt**

|  |  |
| --- | --- |
| Signature | [`STRING_CVT`](../sig/STRING_CVT.md) |
| Status | required |
| Members | 11 |
| Tests | 111 checks |
| Source | [lib/basis/stringcvt.sml](../../../../lib/basis/stringcvt.sml) |

## Synopsis

```sml
structure StringCvt : STRING_CVT
```

StringCvt: the types and helpers of the fmt and scan functions. Written on
primitives, without another structure, because most others require it.

## Members

What each means is on [`STRING_CVT`](../sig/STRING_CVT.md); the types are this structure's own.

|  | Member | Is |
| --- | --- | --- |
| type | [`cs`](../sig/STRING_CVT.md#type-cs) | *a type of its own* |
| datatype | [`radix`](../sig/STRING_CVT.md#type-radix) | `BIN` &#124; `OCT` &#124; `DEC` &#124; `HEX` |
| type | [`reader`](../sig/STRING_CVT.md#type-reader) | `'b -> ('a * 'b) option` |
| datatype | [`realfmt`](../sig/STRING_CVT.md#type-realfmt) | `SCI` &#124; `FIX` &#124; `GEN` &#124; `EXACT` |
| val | [`dropl`](../sig/STRING_CVT.md#val-dropl) | `(char -> bool) -> ('a -> (char * 'a) option) -> 'a -> 'a` |
| val | [`padLeft`](../sig/STRING_CVT.md#val-padleft) | `char -> int -> string -> string` |
| val | [`padRight`](../sig/STRING_CVT.md#val-padright) | `char -> int -> string -> string` |
| val | [`scanString`](../sig/STRING_CVT.md#val-scanstring) | `((cs -> (char * cs) option) -> cs -> ('b * cs) option) -> string -> 'b option` |
| val | [`skipWS`](../sig/STRING_CVT.md#val-skipws) | `('c -> (char * 'c) option) -> 'c -> 'c` |
| val | [`splitl`](../sig/STRING_CVT.md#val-splitl) | `(char -> bool) -> ('d -> (char * 'd) option) -> 'd -> string * 'd` |
| val | [`takel`](../sig/STRING_CVT.md#val-takel) | `(char -> bool) -> ('e -> (char * 'e) option) -> 'e -> string` |

## Notes

### padLeft

> **Reading** `StringCvt.padLeft/width-minInt`. For the smallest `int` the
> difference `i - size s` does not exist as an `int`; `s` is returned all
> the same, and [`Overflow`](../sig/GENERAL.md#exn-overflow) is not raised.

### realfmt

> **Reading** `StringCvt.SCI/carries-negative`. A constructor carries any
> `int option`, also one that no format accepts, such as `SCI (SOME ~1)`:
> [`Size`](../sig/GENERAL.md#exn-size) is raised by `fmt`, not by the constructor.

### splitl

> **Reading** `StringCvt.splitl/reads-no-further-than-first-failing`. "Will
> often use lookahead characters" is taken to mean exactly one: the
> character that stops the scan is read from the source, and nothing after
> it.

<details><summary>Other implementations (2)</summary>

- **SML/NJ** &mdash; padLeft and padRight raise Overflow for the smallest int (they compute i - \| s \| ) instead of returning s
- **SML/NJ** &mdash; skipWS skips space, tab and newline only, not carriage return, vertical tab and formfeed

</details>

---

<sub>Generated by runedoc from lib/basis/stringcvt.sml; do not edit.</sub>
