# structure OS.Process

[The Standard ML Basis Library](../README.md) &rsaquo; The operating system &rsaquo; [Structures](../structures.md) &rsaquo; **OS.Process**

|  |  |
| --- | --- |
| Signature | [`OS_PROCESS`](../sig/OS_PROCESS.md) |
| Status | required |
| Members | 10 |
| Tests | 35 checks |
| Source | [lib/basis/os.sml](../../../../lib/basis/os.sml) |

## Synopsis

```sml
structure OS.Process : OS_PROCESS
```

## Members

What each means is on [`OS_PROCESS`](../sig/OS_PROCESS.md); the types are this structure's own.

|  | Member | Is |
| --- | --- | --- |
| type | [`status`](../sig/OS_PROCESS.md#type-status) | *a type of its own* |
| val | [`atExit`](../sig/OS_PROCESS.md#val-atexit) | `(unit -> unit) -> unit` |
| val | [`exit`](../sig/OS_PROCESS.md#val-exit) | `status -> 'a` |
| val | [`failure`](../sig/OS_PROCESS.md#val-failure) | `status` |
| val | [`getEnv`](../sig/OS_PROCESS.md#val-getenv) | `string -> string option` |
| val | [`isSuccess`](../sig/OS_PROCESS.md#val-issuccess) | `status -> bool` |
| val | [`sleep`](../sig/OS_PROCESS.md#val-sleep) | `Time.time -> unit` |
| val | [`success`](../sig/OS_PROCESS.md#val-success) | `status` |
| val | [`system`](../sig/OS_PROCESS.md#val-system) | `string -> status` |
| val | [`terminate`](../sig/OS_PROCESS.md#val-terminate) | `status -> 'b` |

## Notes

### atExit

> **Implementation** `OS.Process.atExit/how-actions-run`. The actions run in
> the reverse of the order they were given in, at a normal end and at
> [`exit`](../sig/OS_PROCESS.md#val-exit) but not at [`terminate`](../sig/OS_PROCESS.md#val-terminate) and not after an uncaught exception. The
> exception of an action that raises is dropped and the others run, and
> an action that calls [`atExit`](../sig/OS_PROCESS.md#val-atexit) registers nothing.

### exit

> **Reading** `OS.Process.exit/what-is-flushed`. "Flushes and closes all I/O
> streams" reaches the streams the library still holds output for, over
> writers a program supplied; output to a file is held by the VM, which
> flushes every file itself.

### getEnv

> **Reading** `OS.Process.getEnv/the-whole-name`. The whole name must match:
> a name that merely begins with one that is set does not, and neither
> does a name with `"=value"` attached. A command run by [`system`](../sig/OS_PROCESS.md#val-system) inherits
> this environment and cannot change it.

### isSuccess

> **Reading** `OS.Process.isSuccess/killed-is-not-success`. A command that a
> signal ended has not succeeded: from [`UNIX`](../sig/UNIX.md), this is true only where
> [`Posix.Process.fromStatus`](../sig/POSIX_PROCESS.md#val-fromstatus) gives `W_EXITED`.

### status

> **Implementation** `OS.Process.status/of-a-command`. The status of a
> command that [`system`](../sig/OS_PROCESS.md#val-system) ran is its exit code, or 256 plus the number of
> the signal that ended it, which [`Posix.Process.fromStatus`](../sig/POSIX_PROCESS.md#val-fromstatus) decodes.

### system

> **Reading** `OS.Process.system/a-real-shell`. A shell runs the command, so
> redirection, sequencing and variables work; it runs in the current
> directory, and [`system`](../sig/OS_PROCESS.md#val-system) returns only once the command is done. What the
> process has buffered is neither lost nor written twice by running one.

---

<sub>Generated by runedoc from lib/basis/os.sml; do not edit.</sub>
