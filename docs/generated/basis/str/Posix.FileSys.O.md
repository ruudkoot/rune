# structure Posix.FileSys.O

[The Standard ML Basis Library](../README.md) &rsaquo; The operating system &rsaquo; [Structures](../structures.md) &rsaquo; **Posix.FileSys.O**

|  |  |
| --- | --- |
| Signature | [`BIT_FLAGS`](../sig/BIT_FLAGS.md) |
| Status | optional |
| Members | 15 |
| Tests | 53 checks |
| Source | [lib/basis/posix\_filesys.sml](../../../../lib/basis/posix_filesys.sml) |

## Synopsis

```sml
structure Posix.FileSys.O : BIT_FLAGS
```

The flags of open and the bits of a mode, as words. "all represents
the union of all flags", also those of the system that O does not
name (O\_CLOEXEC, and O\_LARGEFILE, which getfl reports): the bits of a
C int. fromWord keeps the bits of all, so that "toWord o fromWord" is
"fn w =\> SysWord.andb (w, toWord all)".

## Members

What each means is on [`BIT_FLAGS`](../sig/BIT_FLAGS.md) and [`POSIX_FILE_SYS`](../sig/POSIX_FILE_SYS.md); the types are this structure's own.

|  | Member | Is |
| --- | --- | --- |
| type | [`flags`](../sig/BIT_FLAGS.md#val-flags) | *a type of its own* |
| val | [`all`](../sig/BIT_FLAGS.md#val-all) | `flags` |
| val | [`allSet`](../sig/BIT_FLAGS.md#val-allset) | `flags * flags -> bool` |
| val | [`anySet`](../sig/BIT_FLAGS.md#val-anyset) | `flags * flags -> bool` |
| val | [`append`](../sig/POSIX_FILE_SYS.md#val-o.append) | `flags` |
| val | [`clear`](../sig/BIT_FLAGS.md#val-clear) | `flags * flags -> flags` |
| val | [`excl`](../sig/POSIX_FILE_SYS.md#val-o.excl) | `flags` |
| val | [`flags`](../sig/BIT_FLAGS.md#val-flags) | `flags list -> flags` |
| val | [`fromWord`](../sig/BIT_FLAGS.md#val-fromword) | `word -> flags` |
| val | [`intersect`](../sig/BIT_FLAGS.md#val-intersect) | `flags list -> flags` |
| val | [`noctty`](../sig/POSIX_FILE_SYS.md#val-o.noctty) | `flags` |
| val | [`nonblock`](../sig/POSIX_FILE_SYS.md#val-o.nonblock) | `flags` |
| val | [`sync`](../sig/POSIX_FILE_SYS.md#val-o.sync) | `flags` |
| val | [`toWord`](../sig/BIT_FLAGS.md#val-toword) | `flags -> word` |
| val | [`trunc`](../sig/POSIX_FILE_SYS.md#val-o.trunc) | `flags` |

## Notes

### O.noctty

> **Reading** `Posix.FileSys.O.noctty/only-for-terminals`. A regular file
> never becomes a controlling terminal, so on one this flag is only
> checked to leave reading as it was.

<details><summary>Other implementations (3)</summary>

- **MLton** &mdash; allSet (fl1, fl2) tests whether fl2 is included in fl1, the other way round from "returns true if all of the flags in fl1 are also in fl2"
- **MLton** &mdash; all of FileSys.O, IO.FD, IO.O and Process.W has every bit of the 64-bit SysWord.word, but fromWord keeps only the 32 of a C int, so that toWord o fromWord is not fn w =\> SysWord.andb (w, toWord all)
- **Poly/ML** &mdash; fromWord keeps every bit of its argument, also those not in all, so that toWord o fromWord is not fn w =\> SysWord.andb (w, toWord all) and fromWord makes flags outside all

</details>

---

<sub>Generated by runedoc from lib/basis/posix\_filesys.sml; do not edit.</sub>
