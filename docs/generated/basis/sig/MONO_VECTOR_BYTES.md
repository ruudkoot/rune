# signature MONO_VECTOR_BYTES

[The Standard ML Basis Library](../README.md) &rsaquo; **MONO_VECTOR_BYTES**

|  |  |
| --- | --- |
| Status | required |
| Implementations | none |
| Documentation | 0 of 2 entries documented |
| Tests | 0 checks of 0 entries |
| Source | [lib/basis/mono\_sigs.sml](../../../../lib/basis/mono_sigs.sml) |

## Synopsis

```sml
signature MONO_VECTOR_BYTES
```

[`Word8Vector`](../sig/MONO_VECTOR.md), as the library sees it: the members of the specification and
the two conversions that say a vector of bytes is a string underneath. A
program sees [`MONO_VECTOR`](../sig/MONO_VECTOR.md), which the seal file gives it. The slice's own
signature is in word8vector.sml, where [`Substring`](../sig/SUBSTRING.md) is in scope.

## Interface

<pre>
signature MONO_VECTOR_BYTES =
sig
  include MONO_VECTOR_EQ
  val <a href="#val-tostring">toString</a> : vector -&gt; string
  val <a href="#val-fromstring">fromString</a> : string -&gt; vector
end
</pre>

**Included from [`MONO_VECTOR_EQ`](../sig/MONO_VECTOR_EQ.md)**: `include MONO_VECTOR_EQ`

| Member |  |  |
| --- | --- | --- |
| [`vector`](../sig/MONO_VECTOR_EQ.md#type-vector) | eqtype | The type of these vectors, which admits equality. |
| [`elem`](../sig/MONO_VECTOR_EQ.md#type-elem) | type | The type of the elements: `WideChar.char` for `WideCharVector`. |
| [`maxLen`](../sig/MONO_VECTOR_EQ.md#val-maxlen) | val | The greatest length such a vector may have. |
| [`fromList`](../sig/MONO_VECTOR_EQ.md#val-fromlist) | val | `fromList l` is the sequence of the elements of `l`, in order. |
| [`tabulate`](../sig/MONO_VECTOR_EQ.md#val-tabulate) | val | `tabulate (n, f)` is the sequence of `f 0`, ..., `f (n - 1)`, applied in order. |
| [`length`](../sig/MONO_VECTOR_EQ.md#val-length) | val | `length x` is the number of elements. |
| [`sub`](../sig/MONO_VECTOR_EQ.md#val-sub) | val | `sub (x, i)` is the element at position `i`, counting from 0. |
| [`update`](../sig/MONO_VECTOR_EQ.md#val-update) | val | `update (v, i, x)` is a new vector like `v` but with `x` at position `i`. |
| [`concat`](../sig/MONO_VECTOR_EQ.md#val-concat) | val | `concat l` is the vectors of `l` one after another. |
| [`appi`](../sig/MONO_VECTOR_EQ.md#val-appi) | val | `appi f x` applies `f` to the index and the element of each position, from 0 up, for its effect. |
| [`app`](../sig/MONO_VECTOR_EQ.md#val-app) | val | `app f x` applies `f` to every element, from 0 up, for its effect. |
| [`mapi`](../sig/MONO_VECTOR_EQ.md#val-mapi) | val | `mapi f v` is the vector of the results of `f` on the index and the element of each position. |
| [`map`](../sig/MONO_VECTOR_EQ.md#val-map) | val | `map f v` is the vector of the results of `f` on each element, in order. |
| [`foldli`](../sig/MONO_VECTOR_EQ.md#val-foldli) | val | `foldli f init x` combines the elements from the left, giving `f` the index as well. |
| [`foldri`](../sig/MONO_VECTOR_EQ.md#val-foldri) | val | `foldri f init x` combines the elements from the right, giving `f` the index as well. |
| [`foldl`](../sig/MONO_VECTOR_EQ.md#val-foldl) | val | `foldl f init x` combines the elements from the left, as `List.foldl` does. |
| [`foldr`](../sig/MONO_VECTOR_EQ.md#val-foldr) | val | `foldr f init x` combines the elements from the right, as `List.foldr` does. |
| [`findi`](../sig/MONO_VECTOR_EQ.md#val-findi) | val | `findi p x` is `SOME (i, e)` for the first position whose index and element satisfy `p`, or `NONE`. |
| [`find`](../sig/MONO_VECTOR_EQ.md#val-find) | val | `find p x` is `SOME e` for the first element that satisfies `p`, or `NONE`. |
| [`exists`](../sig/MONO_VECTOR_EQ.md#val-exists) | val | `exists p x` is `true` when some element satisfies `p`. |
| [`all`](../sig/MONO_VECTOR_EQ.md#val-all) | val | `all p x` is `true` when every element satisfies `p`. |
| [`collate`](../sig/MONO_VECTOR_EQ.md#val-collate) | val | `collate cmp (a, b)` compares the elements of two of these lexicographically with `cmp`. |

### <a name="val-tostring"></a>`toString`

```sml
val toString : vector -> string
```

### <a name="val-fromstring"></a>`fromString`

```sml
val fromString : string -> vector
```

---

<sub>Generated by runedoc from lib/basis/mono\_sigs.sml; do not edit.</sub>
