# Benchmarks

Benchmarks cover compiler phases, bytecode execution, startup, allocation, and
representative programs. Each benchmark documents its workload, environment
assumptions, metric, and regression threshold.
